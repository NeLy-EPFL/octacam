[CONFIRMED] (strand-cam) strand-braid is dual-licensed MIT OR Apache-2.0 (permissive, not AGPL); an academic lab can fork it freely.
CORRECTION: Confirmed as stated, with one minor nuance: strand-braid (including the strand-cam crate) is dual-licensed MIT OR Apache-2.0 at the user's option — permissive, not AGPL — so an academic lab can fork it freely. Minor caveat: a small number of crates/files derived from third-party code carry BSD-2-Clause or BSD-3-Clause licenses instead, which are also permissive and do not restrict forking.
EVIDENCE: Primary sources from the official strawlab/strand-braid GitHub repo: (1) README.md license section (https://github.com/strawlab/strand-braid/blob/main/README.md): "Except where noted otherwise (in individual files and crates), this open-source software is dual licensed under either of - Apache License, Version 2.0, (LICENSE-APACHE or http://www.apache.org/licenses/LICENSE-2.0) - MIT license (LICENSE-MIT or http://opensource.org/licenses/MIT) at your option." It also notes some third-party-derived files carry BSD-2-Clause/BSD-3-Clause. (2) COPYRIGHT file (https://raw.githubusercontent.com/straw

[CONFIRMED] (strand-cam) pylon-cxx and pylon-shimload are both dual MIT/Apache-2.0; the Rust license does not cover the underlying proprietary Pylon SDK.
CORRECTION: No correction needed. Both pylon-cxx (latest 0.4.4) and pylon-shimload (latest 0.1.1) are dual-licensed "MIT OR Apache-2.0" per their crates.io license fields and per the LICENSE-MIT/LICENSE-APACHE files in their respective GitHub repos (strawlab/pylon-cxx and strawlab/pylon-shimload — note pylon-shimload lives in its own repo, not inside pylon-cxx). Both READMEs explicitly state the license covers only the Rust crate and that the underlying Basler Pylon SDK has different (proprietary) license terms.
EVIDENCE: 1) crates.io registry API https://crates.io/api/v1/crates/pylon-cxx — license field: "MIT OR Apache-2.0", repository: https://github.com/strawlab/pylon-cxx. 2) crates.io registry API https://crates.io/api/v1/crates/pylon-shimload — license field: "MIT OR Apache-2.0", repository: https://github.com/strawlab/pylon-shimload. 3) https://github.com/strawlab/pylon-cxx — contains LICENSE-APACHE and LICENSE-MIT files; README License section (verbatim from https://raw.githubusercontent.com/strawlab/pylon-cxx/main/README.md): "Licensed under the Apache License, Version 2.0 or the MIT license, at your op

[CONFIRMED] (strand-cam) MP4 saving supports Mp4Codec variants H264NvEnc (NVIDIA NVENC, NV12), H264OpenH264 (software), H264LessAvc (pure-Rust LOSSLESS H.264), and H264RawStream passthrough.
CORRECTION: The claim is accurate as stated. The Mp4Codec enum in strawlab/strand-braid (camera/strand-cam-remote-control/src/lib.rs) defines exactly four variants: H264NvEnc(NvidiaH264Options) ("Encode data with Nvidia's NVENC"), H264OpenH264(OpenH264Options) ("Encode data with OpenH264", a software encoder), H264LessAvc ("Encode data with LessAVC"), and H264RawStream ("Data is already encoded as a raw H264 stream", i.e., passthrough without re-encoding). The mp4-writer crate implements all four paths, converting frames to NV12 pixel format for the NVENC path; less-avc is strawlab's pure-Rust ("no use of unsafe") H.264 encoder optimized for lossless encoding.
EVIDENCE: 1) https://raw.githubusercontent.com/strawlab/strand-braid/main/camera/strand-cam-remote-control/src/lib.rs — defines enum Mp4Codec with all four claimed variants verbatim: "H264NvEnc(NvidiaH264Options)" ("Encode data with Nvidia's NVENC"), "H264OpenH264(OpenH264Options)" ("Encode data with OpenH264"), "H264LessAvc" ("Encode data with LessAVC"), "H264RawStream" ("Data is already encoded as a raw H264 stream"). NvidiaH264Options has bitrate and cuda_device fields; OpenH264Options has debug and preset fields. 2) https://raw.githubusercontent.com/strawlab/strand-braid/main/media-utils/mp4-writer/

[CONFIRMED] (strand-cam) less-avc is a pure-Rust lossless H.264 encoder (workspace dep less-avc 0.1.4) usable for scientific grayscale without chroma-subsampling loss; openh264 0.8 is the software encoder; native NVENC feeds NV12.
CORRECTION: The claim is accurate as stated. Minor precision notes: the workspace pin is "0.1.4" (semver-compatible, resolves to >=0.1.4 <0.2), and less-avc is lossless-only/all-intra (8-bit and 12-bit, 4:0:0 monochrome or 4:2:0 color) — it is not a general-purpose lossy H.264 encoder. openh264 and nvenc are optional features in mp4-writer; the openh264 path encodes via Y4M C420paldv (4:2:0), so only the less-avc path avoids chroma subsampling for grayscale.
EVIDENCE: 1) https://raw.githubusercontent.com/strawlab/strand-braid/main/Cargo.toml — workspace dependencies include: less-avc = "0.1.4", openh264 = "0.8.0", nvenc = { path = "media-utils/nvenc" }. 2) https://github.com/strawlab/strand-braid/blob/main/media-utils/mp4-writer/Cargo.toml — "less-avc.workspace = true", "openh264 = { workspace = true, optional = true }", "nvenc = { workspace = true, optional = true }" (plus dynlink-cuda/dynlink-nvidia-encode for NVIDIA). 3) https://github.com/strawlab/less-avc README — "Pure rust. No use of `unsafe`."; "Lossless encoding of 8 bit and 12 bit monochrome (lumi

[CONFIRMED] (strand-cam) A single Turing-generation NVENC (T400/TU117) cannot sustain ~1200 fps of 1080p H.264 (8 cameras x 150 fps), so adopting strand-cam does not solve the user's encoder-throughput bottleneck; this is an inference from NVENC hardware limits, not stated by strawlab.
CORRECTION: Confirmed, with one strengthening precision: NVIDIA's official NVENC Application Note (per-NVENC-engine figures) shows the full Turing NVENC tops out at 855 fps for 1080p H.264 even at the fastest preset (P1, CBR low-latency) measured on an RTX 8000 at its maximum 1950 MHz video clock; quality presets run 229-600 fps. So ~1200 fps of 1080p H.264 exceeds a single Turing engine by ~40% at best. The T400 is even further from sufficing: per NVIDIA's own capability tables, TU117 carries the Volta-class NVENC ("Volta and TU117 GPUs" are one column), whose H.264 throughput NVIDIA says equals Pascal fps scaled by clock (Pascal P1 = 667 fps at 1708 MHz); at the T400's lower clocks that is roughly 500-600 fps, under half the required rate. NVIDIA's support matrix confirms the T400 has exactly 1 NVENC chip, so no multi-engine aggregation is possible. The strawlab strand-braid README makes no encoder-throughput claims, confirming this is an inference from NVIDIA hardware limits, not a strawlab statement. Minor caveat: the claim's cited support-matrix URL only proves the 1-engine count; the fps limits come from the NVENC Application Note. Practical note: the bottleneck applies to one T400 specifically; a multi-NVENC GPU (e.g., 2-3 engine Ada/Blackwell parts) or splitting cameras across encoders could reach 1200 fps aggregate.
EVIDENCE: PRIMARY SOURCES: (1) NVIDIA NVENC Application Note, Video Codec SDK 13.0, https://docs.nvidia.com/video-technologies/video-codec-sdk/13.0/nvenc-application-note/index.html — "The encode performance listed... is given per NVENC engine"; H.264 1080p Turing column (RTX 8000): P1 CBR 855 fps, P1 VBR 833 fps, P3 600/602, P5 271/264, P7 229/207; "All measurements are done at the highest video clocks as reported by nvidia-smi (i.e. 1708 MHz, 1950 MHz... for GTX 1060, RTX 8000...)"; "The performance should scale according to the video clocks"; capability tables use the unified column header "Volta and

[CONFIRMED] (rust-stack) openh264 crate is maintained (v0.9, compiles Cisco source, BSD-2) but its own benchmark (~3.9 ms encode at 512x512 on a Ryzen 9 7950X3D) extrapolates to roughly 30 fps/core at 1080p — insufficient for 1200 fps aggregate on a 12-core CPU
CORRECTION: The claim is accurate as stated. Minor refinement: the latest release is v0.9.3 (published 2026-01-21 on crates.io), and the ~3.9 ms figure (3,954,905 ns/iter) is the default-build benchmark; the repo's NASM-assembled build is ~2.2x faster (1,828,287 ns ≈ 1.83 ms at 512x512), which extrapolates to ~69 fps/core at 1080p → ~830 fps aggregate on 12 cores — still insufficient for 1200 fps, so the conclusion holds even in the best case. Note the extrapolation is pixel-count-based (7.91x) and the benchmark is explicitly single-threaded on a Ryzen 9 7950X3D under Windows 11.
EVIDENCE: 1) Repo README (https://github.com/ralfbiedert/openh264-rs, raw: https://raw.githubusercontent.com/ralfbiedert/openh264-rs/master/README.md): benchmark quoted verbatim — "test encode_512x512_from_yuv ... bench: 3,954,905.00 ns/iter (+/- 566,698.00)" (default) and "1,828,287.50 ns/iter (+/- 190,976.50)" (with NASM), measured on "Ryzen 9 7950X3D, Windows 11, single threaded de- and encoding". License section: "OpenH264 core library is BSD-2, Cisco. Wrapper code is BSD-2, Ralf Biedert." Version history: "v0.9 - Edition change to 2024." Default build "Uses the bundled OpenH264 source" (Cisco upstr

[CONFIRMED] (rust-stack) less-avc (strawlab) is a pure-Rust zero-unsafe H.264 encoder supporting 8-bit/12-bit monochrome 4:0:0, but it is lossless-only with PCM-style intra coding (no rate control, no lossy mode), so it provides essentially no compression ratio
CORRECTION: The claim is accurate, with one minor omission: less-avc (strawlab) is a pure-Rust, no-unsafe (#![deny(unsafe_code)]) H.264 encoder supporting lossless encoding of 8-bit and 12-bit monochrome (4:0:0) data — and additionally 4:2:0 YCbCr color, which the claim did not mention. It is lossless-only: every frame is an All-Intra I-frame encoded as I_PCM (the only variant in its MacroblockType enum), with no transform/quantization, no rate control, and no lossy mode (pic_init_qp_minus26 exists in the PPS struct but is hardcoded to zero and unused). Because I_PCM stores raw samples plus container/NAL overhead, the compression ratio is essentially 1:1 (slightly worse than raw). Dual-licensed MIT OR Apache-2.0.
EVIDENCE: Primary source 1 — repo README (https://github.com/strawlab/less-avc): "Pure rust. No use of `unsafe`."; "Lossless encoding of 8 bit and 12 bit monochrome (luminance only 4:0:0) and color (4:2:0) data."; "Encodes using ALL-Intra, also called All-I. Every frame is recorded as an I (intra) frame (also 'keyframe') using PCM encoding."; dual-licensed Apache-2.0 OR MIT. Primary source 2 — actual source code (https://raw.githubusercontent.com/strawlab/less-avc/main/src/lib.rs): crate doc states "In the present implementation, all data is encoded as a lossless PCM frame. (Future updates could include

[CONFIRMED] (rust-stack) Current FFmpeg libx264 supports native monochrome encoding: gray and gray10le are listed among libx264's supported pixel formats (High 4:0:0 profile), so grayscale frames need no fake chroma planes when going through FFmpeg
CORRECTION: Claim is correct in substance. FFmpeg's libx264 wrapper natively accepts gray (8-bit) and gray10le (10-bit) pixel formats and passes them to x264 as X264_CSP_I400 (monochrome 4:0:0), so no fake chroma planes are needed. One pedantic nuance: "High 4:0:0" is not an official H.264 profile name — monochrome is signaled as the standard High profile (High 10 for 10-bit) with chroma_format_idc = 0 (4:0:0 chroma sampling). Requires x264 with X264_CSP_I400 (build >= 153, June 2017); all current builds (e.g. build 164) and even FFmpeg 4.4 (2021) qualify.
EVIDENCE: PRIMARY SOURCES: (1) FFmpeg master source, libavcodec/libx264.c (https://raw.githubusercontent.com/FFmpeg/FFmpeg/master/libavcodec/libx264.c): pix_fmts_all array contains "#ifdef X264_CSP_I400 / AV_PIX_FMT_GRAY8, / AV_PIX_FMT_GRAY10, / #endif" and convert_pix_fmt() has "case AV_PIX_FMT_GRAY8: case AV_PIX_FMT_GRAY10: return X264_CSP_I400;" — gray frames map directly to x264's monochrome colorspace. (2) x264 official source mirror, x264.h (https://raw.githubusercontent.com/mirror/x264/master/x264.h): "#define X264_CSP_I400 0x0001 /* monochrome 4:0:0 */" (X264_BUILD 164). (3) x264 encoder/set.c (

[CONFIRMED] (rust-stack) strawlab/strand-braid (strand-cam) is a production Rust camera app with this exact architecture: pylon backend (runtime pylon|vimba selection), Yew/WASM browser UI built with trunk, MP4 recording with always-available openh264 and optional NVENC, and precise timestamps stored in the H.264 stream metadata
CORRECTION: The claim is accurate as stated and verified against the repository source code itself. Minor refinements: (a) the version on main is 1.0.0-rc.2, i.e. a 1.0 release candidate of long-distributed software; (b) besides pylon and vimba there is also a ci2-webcam backend; (c) besides openh264 and NVENC, current versions also support encoding via an ffmpeg-writer crate (NVENC/QuickSync/AMF/software) and lossless H.264 via less-avc — the claim is correct but not exhaustive; (d) the precise per-frame timestamps are stored as MISP "MISPmicrosectime" SEI (UserDataUnregistered) NAL units inside the H.264 stream, plus a JSON H264Metadata SEI carrying the creation time of the first frame.
EVIDENCE: PRIMARY SOURCES (actual repo files):
1. Runtime backend selection — https://github.com/strawlab/strand-braid/blob/main/strand-cam/src/main.rs : "Supports both the Basler Pylon and Allied Vision Vimba backends, selected at runtime via `--camera-backend` (defaulting to pylon)." Same statement in strand-cam/Cargo.toml: "The `strand-cam` executable supports multiple camera backends (Basler Pylon and Allied Vision Vimba), selected at runtime via `--camera-backend`." Dependencies ci2-pylon, ci2-vimba (and ci2-webcam) are unconditional.
2. Rust app — https://github.com/strawlab/strand-braid (96.5% Ru

[REFUTED] (rust-stack) The T400's TU117 die carries the Volta-generation NVENC block (single engine); as a professional card it is exempt from the consumer simultaneous-session cap, but single-engine throughput at 1200 fps 1080p aggregate is unverified and likely insufficient
CORRECTION: Partially correct on silicon, wrong on the session-cap exemption. Correct parts: the T400 is TU117-based (PCI ID 10de:1ff2 = "TU117GL [T400 4GB / T400E]"), and NVIDIA's official Video Encode/Decode GPU Support Matrix lists it with a single NVENC engine of the "6th Gen" (Volta) NVENC — the matrix maps Volta = 6th Gen and mainstream Turing = 7th Gen, and TU117 parts (T400, T600, T1000, GTX 1650 GDDR5) are all listed as 6th Gen. Refuted part: the T400 is NOT exempt from a simultaneous-session cap. NVIDIA's current support matrix lists "Max # of concurrent sessions = 8" for the T400/T600/T1000 — not "Unrestricted" (that label applies only to higher-end pro cards like Quadro RTX 4000/5000 and RTX A4000) — and the consumer GeForce GTX 1650 is actually listed higher, at 12 sessions. For an 8-camera rig, the T400 sits exactly at its 8-session limit. Also, the throughput point is not "unverified": NVIDIA's own NVENC Application Note shows even the faster 7th-gen Turing NVENC (Quadro RTX 8000) peaks at ~855 fps H.264 / ~932 fps HEVC at 1080p fastest preset (P1 CBR), and Pascal-class engines at ~670 fps, so 1200 fps 1080p aggregate on the T400's single, lower-clocked 6th-gen engine is verifiably unattainable per NVIDIA's published numbers.
EVIDENCE: 1) NVIDIA Video Encode and Decode GPU Support Matrix (https://developer.nvidia.com/video-encode-and-decode-gpu-support-matrix-new): T400 row reads "NVIDIA T400 / T400 4GB | Turing | 6th Gen | D | 1 | 1 | 8 | ..." (1 NVENC, max 8 concurrent sessions); "GeForce GTX 1650 GDDR5 | Turing | 6th Gen | ... | 12"; "GeForce GTX 1650 GDDR6 | Turing | 7th Gen | ... | 12"; NVIDIA T600 and T1000: 6th Gen NVENC, 8 sessions; Quadro RTX 4000/5000 and RTX A4000: 7th Gen NVENC, "Unrestricted" sessions; the matrix maps Volta = 6th Gen, Ada = 8th Gen, Blackwell = 9th Gen. 2) PCI ID repository (https://admin.pci-id

[CONFIRMED] (cpp-gstreamer) x264enc accepts GRAY8 input natively (sink caps include GRAY8), enabling true 4:0:0 monochrome H.264 with no chroma conversion
CORRECTION: The claim is accurate. x264enc's sink pad template includes GRAY8, and the plugin source maps GST_VIDEO_FORMAT_GRAY8 directly to X264_CSP_I400 (x264's native 4:0:0 monochrome colorspace, single plane), so no chroma conversion is performed. Minor caveat: GRAY8 is only advertised in the caps when the linked libx264 supports I400 chroma format (gated by the ALLOW_400_8 flag in gst_x264_enc_add_x264_chroma_format(); libx264 gained I400/monochrome support around build 159, released 2019), so a sufficiently recent libx264 is required for GRAY8 to appear at runtime.
EVIDENCE: 1) Official GStreamer x264 plugin documentation (https://gstreamer.freedesktop.org/documentation/x264/index.html) — x264enc sink pad template: "format: { Y444, Y42B, I420, YV12, NV12, GRAY8, Y444_10LE, I422_10LE, I420_10LE }" — GRAY8 is explicitly listed. 2) Actual plugin source code, gstx264enc.c in the official GStreamer monorepo (https://raw.githubusercontent.com/GStreamer/gstreamer/main/subprojects/gst-plugins-ugly/ext/x264/gstx264enc.c, GitHub mirror of gitlab.freedesktop.org/gstreamer/gstreamer) — gst_x264_enc_gst_to_x264_video_format() contains: "case GST_VIDEO_FORMAT_GRAY8: if (nplanes

[CONFIRMED] (cpp-gstreamer) The T400's TU117 chip has a Volta-generation NVENC (no Turing encoder improvements, no HEVC B-frames)
CORRECTION: No correction needed. NVIDIA's official documentation confirms the claim: the T400 (TU117 chip, Turing architecture) has the 6th-generation NVENC — the same generation NVIDIA assigns to Volta (Quadro GV100) — not the 7th-generation Turing NVENC found in TU102/TU104/TU106, and it lacks HEVC B-frame support. NVIDIA's NVENC Application Note explicitly groups the chip under a column titled "Volta and TU117 GPUs". Minor phrasing nuance: NVIDIA's matrix labels it "6th Gen" rather than literally "Volta-generation", but the Volta GV100 is listed as the same 6th Gen, so the equivalence is per NVIDIA's own docs.
EVIDENCE: 1) NVIDIA Video Encode and Decode GPU Support Matrix (official): https://developer.nvidia.com/video-encode-and-decode-gpu-support-matrix-new — row: "NVIDIA T400 / T400 4GB | Turing | 6th Gen | ... HEVC B Frame: NO"; compare "Quadro GV100 | Volta | 6th Gen | ... HEVC B Frame: NO" and full Turing GPUs (e.g., RTX 2060) listed as "Turing | 7th Gen". 2) NVIDIA NVENC Application Note, Video Codec SDK 13.0 (official): https://docs.nvidia.com/video-technologies/video-codec-sdk/13.0/nvenc-application-note/index.html — hardware capabilities table column header reads verbatim "Volta and TU117 GPUs", with

[CONFIRMED] (cpp-gstreamer) NVENC 1080p H.264 P1 throughput is ~667 fps (CBR low-latency) on Pascal and ~855 fps on Turing, with Volta = Pascal x clock ratio; therefore the single-engine, low-clock T400 lands roughly at 500-700 fps — below the 1200 fps aggregate worst case, so NVENC alone cannot carry 8x150fps
CORRECTION: The claim is accurate as stated. Minor refinements from primary sources: (1) NVIDIA's support matrix classifies the T400's NVENC as 6th Gen — the same generation it assigns Pascal (GTX 1060/1080) and Volta (GV100/Titan V) — versus 7th Gen for true Turing NVENC (RTX 2080/Quadro RTX 4000), so using Pascal-x-clock-ratio rather than the 855 fps Turing figure is the correct method for the T400 (TU117). (2) T400 boost clock derived from its official datasheet (1.09 TFLOPS / 384 cores) is ~1.42 GHz, giving 667 x (1425/1708) ≈ 556 fps at 1080p H.264 P1 CBR low-latency; even mis-applying the Turing figure (855 x 1425/1950 ≈ 625 fps) stays in the claimed 500-700 fps band. (3) T400 has exactly 1 NVENC engine and a hard cap of 8 concurrent encode sessions per NVIDIA's matrix. (4) Caveat: the app-note fps figures are for 1920x1080 input, so the 'cannot carry 8x150fps = 1200 fps' conclusion assumes ~1080p-class camera frames (true for 150 fps Basler USB3 models, which are 1920x1080/1920x1200).
EVIDENCE: 1) NVENC Application Note, Video Codec SDK 13.0 (https://docs.nvidia.com/video-technologies/video-codec-sdk/13.0/nvenc-application-note/index.html): 1080p H.264 P1 CBR (low latency) table — Pascal 667 fps (GTX 1060 @ 1708 MHz), Turing 855 fps (Quadro RTX 8000 @ 1950 MHz); "The performance should scale according to the video clocks as reported by nvidia-smi"; Volta performance obtained "by multiplying the Pascal fps in the above table by ratio of the clocks"; figures are per NVENC engine. 2) NVIDIA Video Encode and Decode GPU Support Matrix (https://developer.nvidia.com/video-encode-and-decode-

[CONFIRMED] (cpp-gstreamer) x264 ultrafast 1080p on an i7-8700K encodes ~600 frames in ~4.2s (~145 fps) on complex game content at 6 Mbps, implying 1200 fps aggregate on a 12700KF is achievable only at the fastest presets with grayscale/static content, and must be validated on the rig
CORRECTION: The claim is accurate as stated. The cited repo (cmoore1776/x264-benchmark) reports, for an i7-8700K using all cores (THREADS=0), that encoding a 10-second 3DMark Skydiver action clip at 1920x1080 @ 60 fps (= 600 output frames) with x264 preset ultrafast at 6000K bitrate took 0m 4.15s, i.e. ~144.6 fps — matching "~600 frames in ~4.2s (~145 fps)". Minor precision notes: (1) the measured wall time includes decoding the 1080p120 x265 source, so pure encode throughput could be slightly higher than 145 fps; (2) the "1200 fps aggregate on a 12700KF only at fastest presets with grayscale/static content" portion is an extrapolation, not a measurement — it is directionally sound (12700KF is roughly 2x-2.5x the 8700K in multithreaded encoding, projecting ~300-360 fps on similar complex content, far below 1200 fps) and the claim itself correctly hedges that it must be validated on the rig.
EVIDENCE: Primary source: https://github.com/cmoore1776/x264-benchmark (README fetched raw via GitHub API, default branch "master"). Verbatim quotes: "The benchmark encodes a 10-second action clip from 3DMark Skydiver at 6000K ([Twitch's maximum recommended bitrate](https://stream.twitch.tv/encoding/))"; "Here's some sample results from an i7 8700k utilizing all cores (THREADS=0)"; results table: "resolution: 1920x1080 @ 60 fps / ultrafast: 0m 4.15s / superfast: 0m 5.53s / veryfast: 0m 8.17s / faster: 0m 11.58s / fast: 0m 13.23s / medium: 0m 12.75s / slow: 0m 14.96s"; "For reference, the source file is 